rootProject.name = 'api'

include 'java-vertx-web-with-uuid',
        'server'

dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}